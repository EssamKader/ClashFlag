label: wayfinder:decision
type: Routine

# Tool/bundle naming convention

Low-stakes, deciding without asking: the tool will be internally named `ClashFlag`
(module/class names `ClashFlagOptions`, `ClashFlagRunner`, ticket/branch prefix
`clashflag-`). Exact bundle/folder name (`.pushbutton`, `.extension`, or `.csproj`)
depends on the delivery mechanism chosen in 0005-output-delivery.md, so final path
naming is finalized once that lands — this ticket only locks the tool's short name.

## Status: resolved (decided without asking — low stakes per skill's Routine type)
